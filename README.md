# hamz
build virtial environment by
python -m venv env

activate the environment with
cd env/Scripts/activate

install requirements 
pip install -r requirements.txt

run the app
python manage.py runserver